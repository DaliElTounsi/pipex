NAME pipex
SRC liste des c
BONUS_SRC 
OBJ 
BONUS_OBJ
CFLAGS : -Wall -Wextra -Werror.
LIBFT :

$(NAME) : Compile la partie obligatoire.
all : Alias pour $(NAME).
clean : Supprime les fichiers objets.
fclean : Supprime les objets et l’exécutable.
re : Relance fclean puis all.